from django.apps import AppConfig

#Definicion de las aplicaciones usadas

class MarvelmicroserviceConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'applications.searchComics'
